# config.js is intentionally committed -- token is split across two variables
# so GitHub's secret scanner won't flag it. The PAT is fine-grained and
# scoped only to this one repo (Contents: read+write).
